# spring-testing-study 지침

Spring 테스트 라이브러리 **학습용** 레포다. 프로덕션 코드가 아니라 **예제와 연습문제**가 산출물이다.

## 무엇이 정본인가

**테스트 코드 자체가 문서다.** `src/main` 은 테스트 대상을 만들기 위한 최소한이고,
설명은 테스트 파일의 javadoc 과 각 장 README 에 있다.
새 내용을 추가할 때 문서만 고치고 코드를 안 고치면 안 된다 — **돌아가는 예제가 없는 설명은 넣지 않는다.**

## 태스크 둘의 계약

| 명령 | 대상 | 기대 상태 |
|---|---|---|
| `./gradlew test` | 완성본 (태그 없음) | **항상 초록.** 하나라도 깨지면 그건 버그다 |
| `./gradlew exercise` | `@Tag("exercise")` | **빨간불이 정상.** 푸는 사람이 초록으로 만든다 |

🔥 **연습문제를 `test` 태스크에 섞지 않는다.** CI 가 영원히 빨간불이면
"깨진 게 정상"인 상태에 익숙해져, 진짜 회귀가 났을 때 아무도 알아채지 못한다.

⚠ **연습문제 파일에는 반드시 클래스 레벨 `@Tag("exercise")` 를 붙인다.**
빠뜨리면 `test` 태스크로 새어 들어가 CI 를 깨뜨린다.

## 장을 추가할 때

1. 패키지 `io.study.testing.ch0N_<주제>` — 번호를 붙여야 정렬된다
2. 테스트 대상이 필요하면 `src/main/java/io/study/testing/ch0N_<주제>/`
3. 완성본 테스트 (1~2 파일) — javadoc 에 **함정 위주로** 설명을 단다
4. `exercise/` 하위에 연습문제. 각 문제는 `throw new AssertionError("TODO: ...")` 로 시작
5. 그 패키지에 `README.md` — 파일 목록 · 핵심 · 함정
6. 루트 `README.md` 의 목차 표와 테스트 개수 갱신

## 설명을 쓰는 방식

이 레포의 주석은 "이 API 는 이런 겁니다" 가 아니라 **"왜 이렇게 하나 / 안 하면 무슨 일이 나나"** 를 적는다.

- 📌 알아 둘 것
- ⚠ 헷갈리는 자리
- 🔥 실제로 사고가 나는 자리
- 💡 요령

**겪은 실수는 그대로 적어 둔다.** 예: 테스트 메서드 이름을 `assertThatThrownBy` 로 지어
static import 가 가려져 컴파일이 깨진 것 — 이런 것이 가장 값어치 있는 문서다.

## 버전을 올릴 때

Boot 3.4.7 로 **의도적으로 고정**했다(실무 코드베이스와 맞추기 위해). 올리려면 둘을 함께 고친다:

- `build.gradle.kts` 의 `org.springframework.boot` 버전
- `gradle/wrapper/gradle-wrapper.properties` 의 `distributionUrl`

⚠ **Boot 3.4 는 Gradle 9 를 지원하지 않는다.** 한쪽만 올리면 빌드가 깨진다.

## 하지 않는 것

- 실행되지 않는 예제를 문서에만 적는 것
- 한 파일에 장을 여러 개 담는 것 — 장 단위로 읽고 돌릴 수 있어야 한다
- 연습문제의 답을 같은 파일에 적는 것 — 답은 완성본에 있고, README 가 그리로 안내한다
